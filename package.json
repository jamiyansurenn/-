{
  "name": "moncon-monorepo",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix frontend --include=dev && npm run build --prefix frontend && node scripts/sync-root-public.cjs"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^1.3.1",
    "next": "14.2.18",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
