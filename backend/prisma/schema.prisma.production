// Production schema for PostgreSQL
// Copy this to schema.prisma when deploying to Render

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Copy all models from the original schema.prisma
// This is just a reference - use the original schema.prisma and change provider to "postgresql"
